[tool.poetry]
name = "ss-scraper"
version = "0.1.0"
description = "SS.com scraper with optional Supabase/Postgres backend"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "*"
beautifulsoup4 = "*"
SQLAlchemy = "^2.0"
psycopg2-binary = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
